{
  "name": "orcfax-schema",
  "version": "1.0.0",
  "description": "Zivana Orcfax Revenue Event Fact Statement",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:onchain": "cd onchain && aiken build",
    "gen-wallet": "tsx scripts/gen-wallet.ts",
    "publish": "tsx scripts/publish-fact.ts",
    "query": "tsx scripts/query-fact.ts",
    "validate": "tsx scripts/validate-schema.ts",
    "test": "tsx tests/datum.test.ts",
    "test:onchain": "cd onchain && aiken check"
  },
  "dependencies": {
    "@lucid-evolution/lucid": "^0.5.5",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=18"
  }
}
