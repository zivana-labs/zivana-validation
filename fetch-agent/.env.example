# ASI Cloud API Key — get yours from https://asicloud.cudos.org/
# Required for live market price inference. Without it, the agent uses mock mode.
ASI_CLOUD_API_KEY=

# Agent seed phrase — used to generate a deterministic agent address.
# Keep this secret. Change it for production deployments.
AGENT_SEED=zivana-seed
