// A minimal Compact contract for proving a secret number is above a threshold.
contract ThresholdProof {
  // The secret number (private witness)
  private secret: number;
  // The threshold (public)
  public threshold: number;

  // Prove that secret > threshold
  function prove(): bool {
    return this.secret > this.threshold;
  }
}
